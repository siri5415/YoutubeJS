//Arrays

const grocereies = ['graphes','banana','orange','pappyi']
// console.log(grocereies);

//grab the 2nd index
// console.log(grocereies[1]);

//Arrays methods
grocereies.push('cake')
// console.log(grocereies);

grocereies.push('blue')
console.log(grocereies);

//Adds one or more elements to the end of an array and returns the new length of the array.

//The slice() method return the portion of the arrays you want in a new arrays.

// slice the index of the until the inside of groceries[]

//Array Slice()
//starts form 0 inclusive and up to 6 [0,1,2,3,4,5,6]
// console.log(grocereies.slice(0,6));

// console.log(grocereies.slice(3,6)); 



//Arrays methods (slice,push,indexof,length)

//Length is an arrays property that returns the number of elements you have in a given array.
console.log(grocereies.indexOf('banana'));

console.log(grocereies.length);